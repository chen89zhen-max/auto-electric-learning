# Sprint 0 测试与验收

## 本轮执行结果（2026-09-04）

- Vitest：1 个测试文件、6 项测试，全部通过。
- TypeScript：`tsc --noEmit` 通过。
- 代码检查：应用、引擎和测试目录无错误。
- 生产构建：Vinext/Vite 构建通过。
- 依赖审计：升级修复版本后为 0 项已知漏洞。
- 浏览器流程：工单、点击替代、危险操作拦截、安全操作、帮助、复盘、通关、任务大厅和重置均实际走通。
- 响应式检查：1920×1080、1366×768、1024×768 均无横向溢出，主要按钮实测高度约 46 CSS px。
- 未完成：真实学生的 8—10 分钟零说明测试；此项必须线下完成，不能由自动化测试代替。

## 自动测试

运行：

```bash
npm test
```

测试覆盖：

1. 初始电源为 `ON`、护盖为 `CLOSED`。
2. 电源为 `ON` 时尝试打开护盖，护盖保持关闭并记录 `UNSAFE_ACTION_ATTEMPT`。
3. 关闭电源后再次打开护盖，护盖打开并记录 `GUARD_OPEN_SUCCESS`。
4. 请求帮助后，`helpRequested` 与 `tutorPanelOpen` 为真，并依次记录三项帮助事件。
5. 四项目标与两项复盘完成后进入 `COMPLETE`，并记录 `LEVEL_COMPLETE`。
6. 重新开始建立新 session，并恢复全部初始状态。

构建前同时运行：

```bash
npm run typecheck
npm run build
```

## 人工功能验收清单

- 进入页面后只突出当前步骤，非当前功能不可操作。
- 打开工单并点击“开始训练”。
- 用鼠标拖放训练件到收纳位置。
- 重新开始后，改用“点击训练件—点击目标位置”完成同一操作。
- 电源为 `ON` 时直接打开护盖，系统阻止操作且不出现事故、扣分或“错误”字样。
- 观察状态或关闭电源后打开护盖，流程正常继续。
- 在未知符号步骤点击“随便操作”，系统阻止继续；点击“请师傅提示”后出现固定提示。
- 两项复盘选择不合适答案时得到行为反馈；选择核心目标答案后通关。
- 通关页无分数、排名和复杂奖励；任务 1 只显示 `Coming Soon`。
- 点击“重新开始”，检查工单、训练件、电源、护盖、帮助、目标和复盘全部复位。
- 开发模式导出日志，核对事件结构包含时间、session、level、stage、action、payload。

## 尺寸与设备

分别检查：

- 1920×1080 桌面 Chrome
- 1366×768 桌面 Edge
- 常见平板横屏与触控

主要操作按钮的触控区域不得小于约 44×44 CSS px。手机竖屏不是本 Sprint 的重点，但页面应可滚动且不能阻断流程。

## 零说明测试

必须由未使用过系统的学生执行，开发者不能代替。不给口头说明，记录学生能否在 8—10 分钟内独立完成：查看工单 → 放置训练件 → 观察并关闭设备 → 打开护盖 → 请求帮助 → 完成复盘 → 通关。

若需要教师持续站在旁边解释，应判定本次教学验收不通过，并记录卡点后再修改界面。
