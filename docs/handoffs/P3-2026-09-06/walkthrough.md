# P3 验收走查

1. 从课程大厅选择篇章二，逐关完成 B01—B06；每关需完成三个数据片段后才可提交。
2. 每关均点击“查看反例”，核验黄色提示与实验数据相互对应。
3. 在 URL 后附加 `?level=B01`（依次可替换为 B02 至 B06）验证直接进入；使用 `?level=C01` 应返回大厅。
4. B02 改接为并联后总电流为 4A、单灯电压为 12V；B05 重载内阻 0.4Ω 时端电压为 4.2V；B06 的 1kΩ 错误负载把 6V 输出拉至 4V。
