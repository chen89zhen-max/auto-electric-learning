# A02 修正任务移交包（暂停版）

## 【状态】

**PAUSED_DUE_TO_QUOTA**

本任务因 Codex 5 小时额度已用完而暂停，未宣称最终完成。请由 Antigravity 的 Gemini 3.8 Flash 在额度恢复后继续审查、验证和收尾。

## 【任务目标】

修正 A02 实训，使其达到前 3 关的可用水平：

1. 学生可读文字整体放大；
2. 电路图尽量使用中文，隐藏内部英文端子 ID；
3. 红、黑表笔改为点击/拖动连接，不再用菜单选择；
4. 重新梳理每一步，明确“做什么、按什么顺序、完成标准”。

## 【已完成的实现】

### 1. 训练规则与文案模型

文件：`src/levels/a02/a02Training.ts`

- 建立四阶段内容：目标、3 条编号操作、完成标准、提示语和导师反馈。
- 终端采用中文显示名，内部 ID 仅用于规则判断。
- 统一测量快照和记录规则：档位 `DC_V`、红表笔插孔 `V_OHM`、仪表状态 `NORMAL`、端子/开关状态/读数均需匹配。
- 阶段 1 要求正向与反向两次测量；阶段 2 要求开关两端及检修灯两端；阶段 3 要求灯正端、供电端、搭铁端三项证据。
- 提供拖动端子吸附函数，远距离释放不会误连接。

### 2. A02 电路场景交互

文件：`src/levels/a02/scenes/A02VoltageScene.tsx`

- 初始为关机档位 `OFF`，红表笔默认插入 `VΩ`，两支表笔未接端子。
- 表笔支持：点击下方表笔手柄后点击端子、直接拖动到端子、键盘 Enter/Space 操作。
- 连接线、目标高亮、拔下两支表笔和记录按钮均已加入。
- 电路图文字改为中文，保留必要行业符号（`VΩ`、`10A`、`CAT III 600V`）。
- 非当前操作表笔在另一支表笔激活时不拦截端子点击，解决表笔重叠遮挡问题。
- 记录按钮通过纯规则模型校验，错误接线不会推进步骤。

### 3. 外层流程与字号

文件：`src/levels/a02/A02Experience.tsx`

- 改用 `A02_STAGE_CONTENT`，删除重复的旧指导数据。
- 将学生可见的 `SEMI GUIDED`、`SAVED AFTER COMPLETION`、内部基准编码等改为中文或移除。
- 放大退出按钮、结果列表、状态徽章、导师副标题和目标标签。

文件：`app/globals.css`

- 增加 `.a02-shell` 作用域字号规则，扩大场景标题、目标条、反馈、底部栏及提示文字。

### 4. 测试

文件：`tests/a02Training.test.ts`

- 新增 6 项规则测试：无效档位/插孔拒绝、阶段 1 正反向、阶段 2 开关与灯、阶段 3 三项证据、近距离吸附、远距离不吸附。

## 【已验证结果】

以下命令已在暂停前执行，**不需要在本次移交后重复执行**：

- `npm.cmd test -- --run tests/a02Training.test.ts`：6/6 通过。
- `npm.cmd test -- --run tests/a02Training.test.ts tests/multimeter.test.ts`：12/12 通过。
- `npm.cmd run typecheck`：通过。
- `npm.cmd run lint`：通过。
- 已用本地浏览器打开 `http://localhost:3010/?level=A02` 做交互抽查：
  - 直流电压档、红正黑负可记录约 12V；
  - 直接拖动红表笔到开关输入端可吸附；
  - 阶段 1 正向/反向记录后可进入阶段 2；
  - 阶段 2 开关两端及检修灯两端记录后可进入阶段 3；
  - 新版步骤卡显示 3 条编号操作和完成标准。

## 【当前文件状态】

本次涉及的核心文件：

- `src/levels/a02/a02Training.ts`（新增）
- `tests/a02Training.test.ts`（新增）
- `src/levels/a02/scenes/A02VoltageScene.tsx`（重写 A02 场景）
- `src/levels/a02/A02Experience.tsx`（接入新模型、中文化和字号）
- `app/globals.css`（A02 作用域字号）

未创建提交、未执行清理、未修改或删除其他用户文件。工作区原本已有多个未跟踪目录和中文材料（如 `.superpowers/`、`data/`、`scratch/`、`tmp/` 等），请全部保留，**不要执行 `git add .`、清理未跟踪文件或 reset**。

## 【高风险事项与待办】

1. `app/globals.css` 的格式化/换行变化造成较大的 diff（当前统计约 2216 行变化），可能混入与 A02 无关的换行噪声。继续前先检查 `git diff -- app/globals.css`，只保留 `.a02-shell` 相关改动，谨慎处理其他用户改动。
2. 尚未运行 `npm.cmd run build`，额度恢复后再运行完整回归：`npm.cmd test`、`npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run build`。
3. 尚未完整浏览器走完阶段 3（三项证据）和阶段 4（故障诊断）；需验证错误反馈、重复记录和完成态。
4. 已在约 1366×768 视口抽查，字号明显改善，但电路下半部和底部控件需要内层滚动。请在 1366×768 与 1920×1080 检查首屏可见性、滚动和点击区域。
5. `-0.00 V` 的中间错误接线显示曾出现过；当前有效路径未受影响。若要全局修正，必须先补充测试，勿顺手扩大 A02 范围。
6. “重新开始”在完成态是否完全清除外层 `isCompleted` 状态仍建议复核；这是潜在的既有问题，修改前应补测试。
7. 未做移动端/平板触控回归；拖动事件和键盘可达性应抽查。

## 【Gemini 3.8 Flash 接续步骤】

1. 先阅读本文件和上述 5 个核心文件，检查 `git diff`，修复 CSS 换行噪声并确认没有覆盖用户已有改动。
2. 运行完整测试与构建；失败时只修复 A02 范围问题，并补充对应测试。
3. 在本地 A02 页面完成四阶段真实操作回归，记录截图/结果，重点检查字号、中文端子、点击/拖动表笔和步骤清晰度。
4. 通过后由项目负责人决定是否提交；当前没有 commit，也没有向外部系统自动派发任务。

## 【建议验收标准】

- 1366×768 下主要指导文字无需浏览器缩放即可阅读；
- 电路图学生可见端子均为中文或必要电气符号；
- 表笔可点击和拖动到端子，菜单选择不再存在；
- 四阶段每一步均明确目标、操作顺序和完成判据；
- 错误档位、插孔、端子或开关状态不能误推进；
- 自动化测试、类型检查、Lint 和生产构建全部通过。

